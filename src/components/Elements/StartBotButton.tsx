import React from "react";
import { Button } from "../ui/button";

const StartBotButton = () => {
  return (
    <Button className="w-full h-[20svh] bg-green-500 text-4xl hover:bg-green-500/80 hover:scale-105 transition-all ease">
      START
    </Button>
  );
};

export default StartBotButton;
